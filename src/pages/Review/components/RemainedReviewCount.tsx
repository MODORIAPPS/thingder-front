import Typography from "@/components/Typography";
import styled from "@emotion/styled";

const RemainedReviewCount = styled(Typography.Body2)`
    color: #424242;
    margin: 8px;
    text-align: right;
    align-self: flex-end;
`;

export default RemainedReviewCount;